# Required for a useful GitHub API rate limit.
GITHUB_TOKEN=

# Direct Telegram adapter is optional. It is NOT used when delivery_mode=stdout.
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Persistent container data location (used by docker-compose.yml).
RADAR_DATA_DIR=./data
